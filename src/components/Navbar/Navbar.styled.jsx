import styled from '@emotion/styled';

export const List = styled.ul`
  list-style: none;
  width: 296px;
  padding: 0;
  margin: 0;
  display: flex;
  gap: 70px;
  
`;

export const Item = styled.li`
  list-style: none;
  
  
`;